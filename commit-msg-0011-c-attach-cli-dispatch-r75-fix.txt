fix(octo-cli): RFC-0011-c §F.6 R7.5 — attach::handle WalletError mapping dedup

DRY review R7 surfaced 5 findings across 5 lenses.
R7.5 fix sweep applies 1 MED (substrate-faithfulness +
simplification confirmed, other MED + LOWs dismissed
after substrate ground-truth check).

crates/octo-cli/src/commands/agent.rs:

- attach::handle WalletError mapping dedup (MED from
  simplification + substrate-faithfulness lenses): replace
  the two hand-rolled match arms at lines 1465-1486
  (lookup_agent + read_agent_state) with calls to the
  existing `common::map_transition_wallet_error` helper.
  The helper covers AgentNotFound, ForbiddenHolderMismatch,
  Hsm, AuditUnavailable, AlreadyInTransition,
  InvalidStateTransition — strictly broader than the prior
  duplicated mapping which silently collapsed AlreadyInTransition
  + InvalidStateTransition + AuditUnavailable to Internal.
  The helper is the canonical WalletError to OctoCliError
  conversion site, used by run::handle + destroy::handle.
  Future WalletError variants only need one match arm update.

Dismissed findings (after substrate ground-truth check):

- substrate-faithfulness MED on runtime_handle field
  schema violation — R6.5 already removed the field from
  AgentAttachOutput (verified at lines 1637-1669). The
  reviewer misread AgentRunOutput.runtime_handle at line 1305
  (the emit-side struct, which DOES carry the field) as the
  attach-side struct.

- hygiene LOW on bare file path `flags.rs` at RFC line 1197
  — line 1197 does not exist in the RFC (file is 1007 lines),
  no `flags.rs` reference appears anywhere in the RFC.

- simplification LOW on comment trims at lines 879-890 +
  933-936 — these comments document the canonical mapping
  helper + the lifecycle-state semantic and are not
  redundant.

Validation:

- cargo fmt --all clean
- cargo clippy -p octo-cli --all-targets -- -D warnings clean
- cargo test -p octo-cli --lib 293 of 293 tests pass

NO PUSH per feedback_initiation_user_only plus git-workflow.
User owns the remote-write step and the PR step.
