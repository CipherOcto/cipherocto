fix(octo-runtime): RFC-0011-c AttachHandle R16.5 fix sweep

Substrate + CLI envelope + mission YAML + RFC hygiene fixes per
R16 5-len reviewer aggregate on mission
0011-c-octo-runtime-attachhandle-substrate (DRY CLOSED gate
verification round 2).

HIGH (hygiene):
- Mission YAML:233 phantom wiki-link feedback_initiation_user_only
  singular form replaced with canonical memory-card filename per
  existing index entry
- RFC:732 phantom wiki-link no-phantom-mission-pointer singular
  form replaced with canonical memory-card filename

MED (hygiene):
- RFC:759 phantom wiki-link no-parallel-abstractions replaced with
  canonical design-principles section reference
- RFC:791 phantom wiki-link stable-abstractions-principle replaced
  with canonical design-principles section reference
- RFC:794 phantom wiki-link stable-abstractions-principle replaced
  with canonical design-principles section reference
- RFC:859 phantom wiki-link rfc-0011-compatibility-timeline
  replaced with prose form (parent stub-deprecation compatibility
  window)
- RFC:942 phantom wiki-link rfc-0011-compatibility-timeline
  replaced with prose form

MED (simplification):
- Deleted impl From octo_wallet::ed25519_dalek::Signature for
  Signature from crates/octo-runtime/src/handle/mod.rs. Zero
  remaining callers after the sign_attach_handle_payload substrate
  change per grep verification
- Deleted signature_from_dalek_signature test from
  crates/octo-runtime/src/handle/mod.rs. Tested the deleted From
  impl. Net delta one fewer test 73 instead of 74 runtime lib tests

MED (substrate-faithfulness):
- RFC:258 §9.3.6 row rewrote. Primitive renamed from
  octo revoke-attach token-hex to
  octo agent revoke-attach --session-id HEX64 per substrate
  RevokeAttachOutput { session_id_hex process_scoped true } shape
  per RFC-0011-c §F.3
- Mission YAML Sub-step 5 reclassified. (follow-on amendment)
  annotation dropped across §Scope prose plus Sub-step 3 4 5
  headings per substrate and CLI dispatch wiring landed in current
  cycle per octo-cli/src/commands/agent.rs RevokeAttach variant

MED (correctness):
- crates/octo-runtime/src/lib.rs step (c) TTL check now rejects
  reserved sentinel ttl_unix == u64::MAX as fail-CLOSED. Same value
  SystemTime::duration_since UNIX_EPOCH returns on broken wall-clock
  so the rejection path has one uniform upstream signal

LOW (simplification):
- crates/octo-runtime/src/handle/mod.rs keepalive_rx field renamed
  to _keepalive_rx. The leading underscore suppresses the
  false-positive dead-code warning without needing
  #[allow(dead_code)]. Field-init site in HandleInner::new plus two
  doc-comment references updated to match

Verification:
- cargo build -p octo-runtime -p octo-cli: clean
- cargo test -p octo-runtime --lib: 73/73 passed (was 74 minus
  deleted signature_from_dalek_signature test)
- cargo test -p octo-cli --lib: 287/287 passed (unchanged)
- cargo clippy -p octo-runtime -p octo-cli --all-targets -D warnings:
  clean
- cargo fmt --all --check: clean
- npx prettier --write on RFC and mission YAML: clean (RFC
  reformatted by prettier; mission YAML unchanged after prettier)

NO PUSH per feedback_initiation_user_only plus git-workflow.
