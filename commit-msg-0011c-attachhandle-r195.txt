fix(octo-runtime): RFC-0011-c AttachHandle R19.5 fix sweep

Cargo dep rationale + substrate comment + RFC + mission YAML
prose trims + TV envelope corrections per R19 5-len reviewer
aggregate on mission 0011-c-octo-runtime-attachhandle-substrate
(DRY CLOSED gate verification round 5).

HIGH (simplification):
- crates/octo-runtime/src/handle/mod.rs line 50-53 stale
  SessionId doc-comment. The 3-line comment described
  SessionId (defined in this same file at line 60, not
  re-exported from a submodule). Replaced with a single line
  matching the actual pub use: Re-export error envelope
  types for crate-internal use.

HIGH (layer-model):
- crates/octo-runtime/Cargo.toml line 44-50 serde_bytes
  rationale comment. Original claim "Replaces a hand-rolled
  signature_bytes_serde module" was false — the local
  signature_bytes_serde module is still required because
  #[serde(with = "...")] needs a concrete module path and
  const-generic ByteArray<N> cannot be referenced directly.
  Rewrote to describe the composition pattern: workspace
  serde adapter composed inside the local const-generic
  wrapper, per [[cipherocto-design-principles]] §No parallel
  abstractions

MED (layer-model):
- crates/octo-runtime/Cargo.toml line 31-32 blake3 dep
  rationale. Original claim "Layer A primitive re-exported
  to encoding module" was false, no pub use blake3 exists,
  encoding.rs imports blake3::hash directly. Rewrote to
  reflect direct dep following the octo-wallet precedent
  (no re-export).

MED (simplification):
- crates/octo-runtime/src/lib.rs line 124-148 redundant dual
  fail-CLOSED comment block. The two adjacent comment blocks
  (broken-clock fail-CLOSED + TTL sentinel rejection) cross-
  referenced each other and explained the same uniform-signal
  invariant twice. Consolidated into a single 5-line block
  above the now_unix binding.

MED (simplification):
- crates/octo-runtime/src/handle/mod.rs line 454-457 verbose
  "deterministic placeholder" parenthetical (5-clause caveat
  about a future HSM amendment that does not yet exist).
  Compressed to 1 line referencing derive_session_id doc per
  §Deferred ≠ Unspecified discipline.

MED (simplification):
- rfcs/accepted/process/0011-c-agent-lifecycle.md line 794
  defensive "file does not exist" parenthetical. The
  negative assertion about a non-existent octo_wallet::crypto
  module belongs in the mission YAML, not the RFC prose.
  RFC asserts positive substrate contracts only. Removed the
  parenthetical.

MED (simplification):
- missions/claimed/0011-c-octo-runtime-attachhandle-substrate.md
  line 75 meta-prose AC-12 merger bullet. The bullet was a
  reviewer-finding ledger entry masquerading as an AC
  acceptance gate. Deleted per §AC contract (AC bullets are
  substrate-faithful acceptance gates, not review-history
  ledger entries).

MED (correctness):
- missions/claimed/0011-c-octo-runtime-attachhandle-substrate.md
  TV-AGT21 row. The Test Vectors table claimed the vector is
  NEW per §Sub-step 3, but Sub-step 3 itself was reclassified
  to "deferred to follow-on amendment" in R18.5 — the CLI
  clap variant does not yet expose --token-file. Re-labeled
  the row DEFERRED with explicit Depends-on note.

MED (correctness):
- missions/claimed/0011-c-octo-runtime-attachhandle-substrate.md
  TV-AGT22 row. The Test Vector documented exit 0 envelope as
  reachable, but the actual dispatch enforces a mode gate that
  returns ConfirmationRequired (exit 2) in default Human mode
  without --confirm. Updated the row to document the mode-
  gate prerequisite (Dev mode OR --confirm in Human/Ci).

Skipped LOWs (R19 simplification LOWs 6-9) per fix-critical-
and-high-only-until-dry pattern.

Verification:
- cargo build -p octo-runtime -p octo-cli: clean
- cargo test -p octo-runtime --lib: 73/73 passed
- cargo test -p octo-cli --lib: 287/287 passed
- cargo clippy -p octo-runtime -p octo-cli --all-targets -- -D
  warnings: clean
- cargo fmt --all --check: clean
- npx prettier --check on RFC + mission YAML: clean

R19 reviewers aggregate: 0 hygiene + 1 HIGH layer-model + 1
HIGH simplification + 4 MED simplification + 2 MED correctness =
2 HIGH + 6 MED substantive. All landed in this sweep.

NO PUSH per feedback_initiation_user_only plus git-workflow.
