fix(octo-audit-core + octo-audit + octo-cli): RFC-0016-a R6.5 sweep

R6 5-len DRY verification surfaced 7 HIGH plus 6 MED
plus 6 LOW findings across all 5 lenses. Aggregated 4
substantive MED plus 1 substantive MED after cross-
reviewer dedup. This batch lands the mechanical fixes.

MED 13 to 18 pattern count sweep (4 files, 7 doc
sites) per R6-h plus R6-si plus R6-c:

- octo-audit-core/src/error.rs:73 13-pattern to
  18-pattern on PermissionDenied scrubber note
- octo-audit-core/src/error.rs:90 13-pattern to
  18-pattern on ChainHashMismatch scrubber note
- octo-audit/src/lib.rs:78 13 scrubber patterns to
  18 scrubber patterns
- octo-audit/src/scrub_newtypes.rs:4 plus 7 plus
  20 three sites 13-pattern to 18-pattern
- octo-cli/src/error.rs:749 plus 818 plus the
  SinkSpecific doc reconciliation two sites 13 to 18

MED SinkSpecific doc plus code reconciliation per
R6-c:

- octo-cli/src/error.rs:746-754 docstring rewrote
  the misleading every payload-bearing variant
  routes through redact_substrate_error claim.
  The 3 CLI-shape variants use redact_substrate_error
  but SinkSpecific uses sanitize_substrate_error
  plus cap_substrate_payload with the 4-marker
  pattern SQL plus query plus sqlite3_open plus
  crates/octo-. Documented why the SinkSpecific
  channel gets the lighter pipeline.

MED Pattern 18 X.509 cert serial regex relaxation
per R6-c:

- octo-audit/src/scrub.rs:238 plus 239 regex
  relaxed from exactly 64 hex chars to 16 to 64 hex
  chars. RFC-5280 Section 5.1.2.3 permits up to 20
  octets plus 40 hex but observed certs use 8 to 16
  bytes plus 16 to 32 hex. Pattern 1 would not catch
  the 0x prefix boundary either. The 32-char test
  vector still passes.

Verified:

- cargo test octo-audit features
  octo-audit-internal lib 54 passing
- cargo test octo-cli lib 241 passing
- cargo test octo-settlement lib 13 passing
- cargo test octo-audit-core lib 6 passing
- clippy plus D warnings clean across audit-core
  plus audit plus cli plus settlement
- fmt applied. NO PUSH.

Noted out of scope:

- 4 ACCEPTED RFCs reference deleted
  scrub_registry_validate (docs drift) per
  R6-h plus R6-si. Deferred to docs editing cycle.
- LOW HashMismatch naming asymmetry out of scope
  per LOW priority.
- LOW cap plus anchor drift out of scope per LOW
  priority.
- LOW WIF regex 50 char conservative false
  positives out of scope per LOW priority.
- R5-h duplicate import claim REJECTED by R6-c
  per Rust glob semantics. No fix needed.
