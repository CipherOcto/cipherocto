fix(octo-audit + octo-cli): RFC-0016-a R7.5 doc accuracy

R7 5-len DRY verification surfaced 1 HIGH plus 2 MED
plus 1 LOW findings across 5 lenses. Aggregated 1
HIGH plus 2 MED plus 1 LOW after cross-reviewer
dedup. R7-h plus R7-sf both flagged the same HIGH
on the wildcard arm docstring copy-paste; R7-sf
flagged the 3 markers enumeration drift; R7-c
flagged the ordering rationale 64 hex stale drift.

HIGH wildcard arm docstring copy-paste fix per R7-h
plus R7-sf:

- octo-cli/src/error.rs:824-829 the wildcard arm
  docstring claimed sanitize_substrate_error
  applies the 18-pattern scrubber but the impl
  actually applies the lightweight 3-string-marker
  pattern plus the crates/octo- path prefix per
  ERROR_MARKERS. Rewrote to describe the actual
  pipeline faithfully.

MED 3 markers enumeration fix per R7-sf:

- octo-cli/src/error.rs:754-757 the docstring said
  4-marker pattern but listed only 3 string
  markers (the crates/octo- path prefix is handled
  separately by ERROR_MARKERS not as a 4th string
  marker). Rewrote to enumerate 3 string markers
  plus the path prefix handled separately.

LOW ordering rationale 64 hex stale fix per R7-c:

- octo-audit/src/scrub.rs:307 the cascade-order
  rationale said X.509 plus 64 hex but the regex
  was relaxed to 16-64 hex by R6.5. Updated the
  rationale to match the relaxed range.

Verified:

- cargo test octo-audit features
  octo-audit-internal lib 54 passing
- cargo test octo-cli lib 241 passing
- cargo test octo-settlement lib 13 passing
- cargo test octo-audit-core lib 6 passing
- clippy plus D warnings clean across audit plus
  cli
- fmt applied. NO PUSH.

Noted out of scope per R7 user scope decisions:

- 4 ACCEPTED RFCs reference deleted
  scrub_registry_validate (docs drift) per R6-h
  plus R6-si. Deferred to docs editing cycle.
- R7-si returned CLEAN (no simplification
  findings).
- R7-l returned CLEAN (layer model verified
  intact).
