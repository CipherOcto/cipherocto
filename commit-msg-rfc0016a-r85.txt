fix(octo-cli): RFC-0016-a R8.5 doc attribution fix

R8 5-len DRY verification surfaced 1 MED across 5
lenses (R8-c, R8-h, R8-l, R8-si returned CLEAN).
R8-sf flagged a docstring attribution drift.

MED error_marker attribution fix per R8-sf:

- octo-cli/src/error.rs:754-757 the docstring said
  the crates/octo- path prefix was handled
  separately by ERROR_MARKERS but ERROR_MARKERS is
  actually a 3-string array containing only the
  string markers SQL: plus query: plus sqlite3_open.
  The crates/octo- path prefix is handled by a
  separate PATH_PREFIX constant in the impl.
  Rewrote to describe the two mechanisms correctly
  without mentioning the specific constant names
  (the impl splits them).

Verified:

- cargo test octo-audit features
  octo-audit-internal lib 54 passing
- cargo test octo-cli lib 241 passing
- cargo test octo-settlement lib 13 passing
- cargo test octo-audit-core lib 6 passing
- clippy plus D warnings clean across audit plus
  cli
- fmt applied. NO PUSH.
