# TV-RX-2 — RFC-0011-d §11 Test Vectors.
vector: TV-RX-2
description: octo role select nonexistent with --confirm exits 31
schema_version: octo.role.test.v1
invocation:
  args: [role, select, nonexistent-role, --confirm]
expect:
  exit_code: 31
  stderr_contains: "role not found"
