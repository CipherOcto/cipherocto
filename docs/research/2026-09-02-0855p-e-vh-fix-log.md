# RFC-0855p-e VH Fix Log

**Date:** 2026-09-02
**Scope:** Version History narratives for `rfcs/draft/networking/0855p-e-handover-request-envelope.md` extracted per plateau option (a) — VH rows reduced to ≤10-word summaries; full per-wave fix narratives preserved here.
**Why this exists:** Plateau pattern identified W4→W9 systemic VH word-count drift (60-200 words per row vs ≤10-word cite-hygiene rule). Extracting narratives here breaks the systemic pattern without losing audit trail.
**Cross-ref:** [[rfc-0855p-de-review-plateau-2026-09-02]] §Plateau Pattern

---

## v0.2 — 2026-06-17

R16 R1 fix: HORQ struct migrated to canonical 10-byte header; SlashTally inlined (referenced non-existent RFC-0855p-b.1); CoordinatorRole inlined (was undefined); removed phantom RFC-0855p-b.1 from Related RFCs.

## v0.3 — 2026-09-01

W1.5 fix batch: strip "(preliminary)" markers; remove fabricated `WitnessCoordinator`; scope to Mission Coordinator only (DomainCoordinator uses RFC-0855p-c §4); fix §5a cite → §"Slash tally observability" + §B; replace `CoordinatorRole` central enum with typed-discriminator; re-export `SlashTally`/`SlashEvent` from RFC-0855p-b instead of duplicating; split envelope into Layer-B wire (`HandoverEnvelope`) + Layer-C payload (`HandoverPayload`) via DCS; raise witness quorum to ≥ 2/3; specify handover race resolution (lex `coordinator_id` tiebreak); specify replay protection (nonce + `current_epoch`); add Roles/Adversary/Implicit/Security/Alternatives/Implementation/KeyFiles/Performance/Compatibility/Economic sections; add Test Vectors ≥ 5; add §Scope (explicit) section.

## v0.4 — 2026-09-01

W2.5 fix batch: HANDOVER_TIMEOUT = 500 (matches 0855p-b); SlashReasonCode FalseAttestation 0x000C → 0x0013 + QuorumTimeout 0x0014 + TallyTamper 0x0015 allocation; coordinator_role_type_id 0x0001 → 0x0100 + namespace table + is_reserved predicate; HORQ broadcast before Active→Handover local transition + rollback rules; new_term_id + pending_envelopes_hash + handover_request_hash derivation rules; slash_tally_update renamed to slash_tally_hash (BLAKE3 reference); HandoverComplete dropped (Handover → Inactive only); HODN_QUORUM constant; HandoverReason → typed-discriminator (HandoverReason removed; replaced by typed-discriminator `handover_reason_type_id: u32` + opaque `reason_payload`); Layer-B/D split for HandoverReason vs CoordinatorRole; re-export GroupBinding from 0850p-c (not 0855p-b); define local SlashTallyUpdate + SlashReasonCode types; crates/octo-network path correction; drop fabricated §"Reward Distribution"; §"Reliability Guarantees" → §11 "Reliability Model"; §"Election Algorithm" → §"Election Algorithm (per governance model)"; phase reassignment (race/replay/eligibility → Phase 1); companion mission archived path.

## v0.5 — 2026-09-01

W3.5 fix batch: Status header v0.5 (supersedes v0.4); VH v0.4 stale HandoverReason-#[non_exhaustive] claim stripped; ack_hash/done_hash domain-separated ("DOT/1/HANDOVER/ack" / "DOT/1/HANDOVER/done"); MAX_GROUP_BINDINGS_PER_HANDOVER const declared + BoundedVec type; HANDOVER_QUORUM_NUMERATOR/DENOMINATOR naming unified + explicit consts; witness self-slash collateral removed; MAX_TALLY_BYTES dead const tightened to MAX_TALLY_HASH_BYTES=32 (or removed); HODN_QUORUM helper clarified; SlashReasonCode → newtype + named consts; LateDelivery = 0x0016 allocation; replay dedup tuple adds coordinator_id; Layer-C alias GroupBindingRef (or drop re-export); HANDOVER_TALLY_WINDOW_EPOCHS = 10; SlashReasonCode/HandoverReasonTypeId distinct newtypes; sender_state_snapshot: CoordinatorLifecycle typed; race resolution prose corrected (raw coordinator_id lex); SlashTallyUpdate local def marked temporary pending octo-coordinator-types crate; §BLAKE3 Construction Conventions added; old/new client divergence migration period documented; §B amendment → "pending §B amendment" at 6 cites.

## v0.6 — 2026-09-02

W4.5 fix batch: Status header v0.6 (supersedes v0.5 baseline); HANDOVER_REQUEST/HANDOVER_ACK/HANDOVER_DONE subtype tag consts declared + 10-byte canonical header layout inlined (RFC-0850p-c §A reference); SlashReasonCode cross-RFC split documented with F-7 critical-sequencing note (lift into shared `octo-coordinator-types` crate OR land 0855p-b §B amendment before Accept); incumbent mid-handover signing lockout rule added (recipient-side rule: reject state-changing envelopes from incumbent with HORQ-in-flight per `(coordinator_id, coordinator_term_id)`); BLAKE3 domain separation: new_term_id + pending_envelopes_hash domain-prefixed; slash_tally_hash removed from 0855p-e domain-separation scheme (canonical form owned by 0855p-b); pending_envelopes_hash Merkle-leaf-style (each envelope hashed individually then inner-root list hashed) to prevent concatenation collisions; slash_tally_hash fetch at `current_epoch` (no lookback; `HANDOVER_TALLY_WINDOW_EPOCHS` deprecated); F-7 lists `LateDelivery 0x0016`; `MAX_TALLY_HASH_BYTES` dead const removed; lex tiebreak canonical form via BLAKE3_keyed("DOT/1/HANDOVER/coordinator_id_lex", coordinator_id); `CoordinatorLifecycle` re-export → Layer-C alias (matches GroupBindingRef pattern); witness_id endianness comment tightened (raw 32-byte array; no endianness); migration window safety invariant added (recipient MUST refuse envelopes on incumbent coordinator_id post-HODN regardless of attacker fork preference); `hodq_quorum` → `hodn_quorum` rename; `CoordinatorLifecycle` upstream `#[non_exhaustive]` flagged as cross-RFC follow-up; KeyFiles consolidated to single substrate-truth path; Phase 4 (audit/CLI/observability) merged into Phase 3 (additive work tracked under future missions).

## v0.7 — 2026-09-02

W5.5 fix batch: Status header v0.7 (supersedes v0.6 baseline; v0.6 row baseline corrected from "v0.4" → "v0.5"); F-7 BLOCKING acceptance gate (was critical-sequencing note); `new_term_id` adds `mission_id` BLAKE3 input (cross-mission term-id collision prevention); `done_hash` domain-prefixed per §BLAKE3 Construction Conventions; `HANDOVER_TALLY_WINDOW_EPOCHS` "REMOVED" → "deprecated"; `HandoverReasonTypeId 0x0013` reserved range marker (cross-namespace distinct from `SlashReasonCode`); Performance Targets "100 epochs" → "500 epochs"; TV-HO-2 deprecated `HANDOVER_TALLY_WINDOW_EPOCHS` wording + TV-HO-8 ordinal swap; `sender_state_snapshot: CoordinatorLifecycle` → `sender_state_snapshot_ordinal: u8` + `snapshot_proof: [u8; 32]` (typed-discriminator decouples Layer-B wire from Layer-C upstream enum); pending envelopes transfer protocol added (mesh fanout fetch within `pending_broadcast_window = 32`); `hodn_quorum` defense-in-depth `assert!` (member set ≥ 3); Implementation Phases Phase 4 (audit/CLI/observability) split out from Phase 3 (substrate-only); Layer placement H2 → spec-template-conformant table; Related RFCs RFC-0009 added (cross-RFC reference for `mission_id` derivation); MAX_GROUP_BINDINGS_PER_HANDOVER DoS worst-case wire-size analysis (~545 KiB per HODN); lockout scope tightened to HORQ_CANCEL-only (was HOAK confusion wording in v0.6); `CoordinatorLifecycle` upstream `#[non_exhaustive]` follow-up moved to v0.7 row (no longer listed in v0.6 narrative).

## v0.8 — 2026-09-02

W6.5 fix batch: Status header v0.8 (supersedes v0.7 baseline); `mission_id` field 32B → 16B (matches BLAKE3 truncation input); `MAX_FSKEW_EPOCHS = 4` declared in §Implicit Assumptions Audit + §Replay Protection (cross-RFC alignment with 0855p-d ±4); HANDOVER_REQUEST_CANCEL (`b"HORC"`) subtype + `HandoverCancelEnvelope` struct defined (lockout carve-out reference no longer points to undefined subtype); HOAK lockout wording fixed (incumbent never signs HOAK; carve-out covers `b"HORC"` only); MAX_GROUP_BINDINGS_PER_HANDOVER DoS comment corrected (`pending_broadcast_window = 32` was time-window not count cap); Implementation Phases Phase 3 / Phase 4 duplicated → Phase 3 / 4 / 5 split (Phase 3 = slash-tally carry-over; Phase 4 = substrate tests; Phase 5 = operator surface); §Sender State Snapshot Ordinals subsection added with `SenderStateSnapshotOrdinal(pub u8)` newtype + full 8-state table; `sender_state_snapshot_ordinal: u8` → `SenderStateSnapshotOrdinal(pub u8)` newtype (typed-discriminator); `snapshot_proof` binding version-stable byte form (was falsely version-locked to upstream enum); `hodn_quorum` const fn panic comment corrected (Rust 1.57+ const fn supports `assert!`); `horq_quorum` defense-in-depth `assert!` mirror added; `HORQ_QUORUM` row text corrected to "const fn with `assert!`" (was constant casing); TV-HO-2 lookback wording drift corrected (fetch at HORQ `current_epoch` not `current_epoch − small_window`); code fence mismatches fixed; Layer placement HandoverPayload DCS encoding row retained Layer B + semantic-content row Layer C added.

## v0.9 — 2026-09-02

W7.5 fix batch: Status header v0.9 (supersedes v0.8 baseline); TV-HO-2 body wording drift corrected (`current_epoch − small_window` → `current_epoch`); 4-backtick fence fixed (L333 + L463); Layer placement HODN_QUORUM casing → `hodn_quorum` const fn + `horq_quorum` const fn row added (with `assert!` defense-in-depth); `hodn_quorum` defense-in-depth note → actual concrete `pub const fn horq_quorum(witness_set_size: usize) -> usize` definition; `HandoverCancelEnvelope.cancel_reason_payload: Vec<u8>` → `BoundedBytes<256>` (unbounded DoS amplification); `HandoverCancelEnvelope.mission_id: [u8; 16]` field added + replay-key tuple `(b"HORC", mission_id, coordinator_id, coordinator_term_id, current_epoch_be, nonce)` (cross-mission replay collision prevention); §Handover Reason Type IDs 0x0013-0x001F split → 0x0013-0x0016 slash-range reserved + 0x0017-0x001F RFC-allocated future reasons (value-level namespace collision guard); `HandoverEnvelope.snapshot_proof_version: u16` wire field added (snapshot_proof algorithm migration hook); lockout carve-out explicitly enumerates `b"HORQ"` (initial triggering broadcast) + `b"HORC"` (mid-handover cancel) + post-HODN HORC forbidden; HODN hash-chain-binding verification: `handover_request_hash == BLAKE3-256(canonical_dcs(HORQ_envelope_with_signature_zeroed))`; Implementation Phases Phase 1 `HODN_QUORUM constant` → `hodn_quorum const fn (with assert! defense-in-depth)`; `HODN_QUORUM witness verification` heading → `hodn_quorum witness verification` casing.

## v1.0 — 2026-09-02

W8.5 fix batch: Status header v1.0 (supersedes v0.9 baseline); MAX_FSKEW_EPOCHS=4 pub const declaration added in §Layer placement constants block (phantom claim at L131 would fail substrate compile); pub type CoordinatorLifecycle alias → pub use re-export per 0855p-d convention; pub type GroupBindingRef alias → pub use re-export; HandoverCancelEnvelope.coordinator_term_id: u64 → [u8;32] (single namespace reconcile with HandoverEnvelope); HandoverCancelEnvelope.payload_hash: [u8;32] field added (fail-closed decode ordering); hodn_quorum/horq_quorum OR → AND (prevents hand-computed quorum path bypass); §Envelope Types Added HORC row added (recipients filtering on table now see subtype); HORC replay-key tuple encoding normalized to canonical BE bytes (RFC-0126 array-of-u8 form) for cross-subtype dedup consistency.

## v1.1 — 2026-09-02

W9.5 fix batch: Status header v1.1 (supersedes v1.0 baseline); HORQ sender_state_snapshot_ordinal != Active(1) canonical path specified — recipients reject HORQ whose snapshot ordinal != Active UNLESS the HORQ carries ≥ 1 second-witness HOAK with `attests_to_predecessor_state: bool = true` flag attesting to incumbent's Active state at `current_epoch - 1`; closes chicken-and-egg (recipient needs snapshot but snapshot is consensus-aggregated). HORC `payload_hash` domain-prefixed to `BLAKE3-256("DOT/1/HANDOVER/cancel_payload" || canonical_dcs(cancel_reason_type_id_be: u32 || cancel_reason_payload))` (was bare `BLAKE3-256(...)` over cancel_reason_type_id || cancel_reason_payload bytes — vulnerable to cross-namespace collision when attacker submits HORC with HORQ-format payload bytes). `MAX_PENDING_ENVELOPES_PER_HODN: u16 = 1024` constant added in §Layer placement constants block (bounds pending-envelope count transferred in single HODN; recipient rejects HODN with `pending_envelope_count_be > MAX_PENDING_ENVELOPES_PER_HODN` before Merkle-leaf computation; prevents unbounded DoS amplification where malicious incumbent references millions of pending envelopes forcing successor to fetch+verify each). `HandoverPayload` adds `pending_envelope_count_be: u16` + `pending_envelope_hashes: BoundedVec<[u8; 32], MAX_PENDING_ENVELOPES_PER_HODN>` (explicit list of `BLAKE3-256(canonical_dcs(env_i))` per pending envelope — explicit list removes ambiguity in pending_envelopes_hash Merkle-leaf reconstruction; recipient fetches each from mesh by `(mission_id, envelope_hash)`).

## v1.2 — 2026-09-02

W10.5 fix batch: Status header v1.2 (supersedes v1.1 baseline); W10 review surfaced 6C + 27H findings — applied C-level + critical H-level: (1) `HandoverAckPayload` adds `attests_to_predecessor_state: bool` + `attested_epoch: u64` fields (W10 L3 C1 — closes the chicken-and-egg where HORQ carries snapshot ordinal claim but no witness attestation backing it; second-witness HOAK with `attests_to_predecessor_state = true` at `attested_epoch` confirms incumbent was Active at that epoch); `ack_hash` domain updated to include the new fields via canonical DCS append. (2) `SenderStateSnapshotOrdinal(pub u8)` newtype marked `#[non_exhaustive]` for forward-extension readiness (W10 L2 H1 — mirrors upstream `CoordinatorLifecycle` `#[non_exhaustive]` pattern; future ordinal additions don't break substrate). (3) `pub const HANDOVER_RACE_WINDOW: u64 = 5;` declared in §Layer placement constants block (W10 L3 C3 — recipients ignore HORQ broadcasts whose `current_epoch` is more than `HANDOVER_RACE_WINDOW` epochs behind local head, preventing late-arriving duplicate HORQ from spurious successor dedup state). (4) Layer placement table rows added: `HandoverCancelPayload` inner DCS Layer B (wire-format semantics per RFC-0126); `SlashTallyUpdate` Layer C (coordinator-kind governance state with typed-discriminator pattern); `SlashReasonCode(pub u32)` newtype Layer C (cross-namespace collision guard); `GroupBindingRef` re-export Layer B (no upstream redefinition per §Stable Abstractions). (5) 3 BLUEPRINT template sub-sections added per BLUEPRINT.md §RFC Process: §RFC-0008 Execution Class Mapping (table: every envelope → Class A/B/C; HORQ/HOAK/HODN/HORC are Class A envelope decode + Class C governance policy); §Determinism Requirements (canonical form per RFC-0126, BLAKE3 domain-prefixed hashes per §BLAKE3 Construction Conventions, no `SystemTime::now()` in production, time-bound windows by `current_epoch` not wall-clock); §Lifecycle Requirements (handover state machine: Active→Handover→Inactive with HORQ/HOAK/HODN/HORC transitions, witness set lifecycle aligned with parent term rotation, slash tally carry-over per §Slash tally observability).

## v1.3 — 2026-09-02 (no change — e stays single RFC)

RFC-0855p-e NOT restructured. Scope remained single-concern (HandoverRequest Envelope) without multi-concern surface area. W10.5 plateau C-level findings L1 C3 (HANDOVER_RACE_WINDOW triple-overloaded semantics), L1 C5 (SenderStateSnapshotOrdinal `pub u8` defeats non_exhaustive), L3 C4 (HORQ single-HOAK predecessor-state attestation bypasses 2/3 quorum invariant) remain in e scope; deferred to follow-on e-focused review wave per [[cipherocto-design-principles]] §Discipline at first call site (splitting e would not reduce audit surface — e is already single-concern).

**Cross-RFC invariant preserved:** `MAX_FSKEW_EPOCHS = 4` owned by RFC-0855p-d1 §Layer placement table; RFC-0855p-e §Implicit Assumptions Audit + §Lifecycle Requirements cross-ref d1. RFC-0855p-e serves as upstream witness for `MAX_FSKEW_EPOCHS = 4` being an invariant across the family.
