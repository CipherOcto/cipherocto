<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cablescan"
    android:versionCode="2"
    android:versionName="0.2">
    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="28" />

    <!-- Legacy Bluetooth permissions (Android < 12) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Modern Bluetooth scan permission (Android 12+).
         `neverForLocation` flag means we do not derive user location
         from BT scans, so ACCESS_FINE_LOCATION is not required.
         The system auto-grants this for apps targeting SDK < 31 that
         also declare BLUETOOTH/BLUETOOTH_ADMIN (legacy mapping). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <application android:label="CableScan"
                 android:icon="@android:drawable/ic_menu_search"
                 android:allowBackup="false">
        <activity android:name=".MainActivity"
                  android:exported="true"
                  android:label="CableScan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
